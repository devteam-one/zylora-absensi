# Dependencies
node_modules/
**/node_modules/
.pnpm-store/

# Build output (frontend)
dist/
dist-employee/
dist-control/
dist-display/

# Env lokal / rahasia
.env
.env.*.local
.env.local

# Database SQLite backend Zylora (data runtime, jangan di-commit)
server/api/data/

# Packaging desktop/Android (artefak build, dibuat di CI)
desktop-app/www/
desktop-app/dist-installers/
android-app/www/
android-app/android/
# CATATAN: package-lock.json TIDAK lagi diabaikan — harus ter-commit agar CI
# `npm ci` reproducible (build APK/desktop ber-tanda-tangan). Lihat docs/AUDIT-2026-06-28.md.

# IDE
.idea/
.run/

# Design-sync tooling (lihat .design-sync/NOTES.md)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Misc OS
.DS_Store
server/api/version.json
